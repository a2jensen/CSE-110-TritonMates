// stores types ex below
export interface User {
    id: Number,
    username: String,
}

export type event = {
    id: string;
    name: string;
    description: string;
    date: Date;
    event_participants: string[];
};